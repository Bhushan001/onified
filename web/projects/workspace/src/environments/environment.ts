export const environment = {
  apiUrl: 'http://localhost:9080/api', // <-- Replace with your real backend API URL if different
  auth: {
    tokenKey: 'onified-token',
    userKey: 'onified-user',
    refreshTokenKey: 'onified-refresh-token'
  }
}; 